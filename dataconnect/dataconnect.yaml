specVersion: "v1"
serviceId: "flashcard-app"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "flashcard-app-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
